# --- Сборка ---
build/
.vs/
*.user
*.obj
*.pdb
*.ilk
.tools/

# --- Бинарники и runtime (не исходники) ---
*.exe
*.dll
*.gguf

llama/
models/
rag/
dist/

# Данные / конфиг runtime (генерируется рядом с exe; шаблон config.yaml в репо — коммитим)
vectors.bin
chunks.bin
texts.txt
chunks.json

# Локальный config.yaml рядом с собранным exe (не трогаем репозиторный шаблон)
# build/**/config.yaml already under build/

# Документация Office (бинарник)
*.docx

# Логи
*.log
embed_debug.log

# Python (dev-only tools/eval — кэш и venv)
__pycache__/
*.py[cod]
.venv/
venv/
tools/eval/.venv/
tools/eval/venv/

# OS / IDE
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/
*.swp
*~
